class Task {
  constructor(id, description, deadline, priority) {
    this.id = id,
    this.description = description,
    this.deadline = deadline,
    this.priority = priority;
  }
}

export default Task;