class Task {
  constructor(id, description, deadline, priority, etc) {
    this.id = id,
    this.description = description,
    this.deadline = deadline,
    this.priority = priority,
    this.etc = etc;
  }
}

export default Task;